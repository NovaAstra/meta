export function useStore() {
    const getRange = () => {
        return [0, 1]
    }

    const getSizes = () => { }

    const update = () => { }

    return {
        getRange,
        getSizes,
        update
    }
}